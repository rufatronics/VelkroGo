// Command velkro is the VelkroGo client: it provides the TUI (and CLI entry
// points) that connect to the velkrod daemon over its local API. The GUI is
// served separately by the daemon as a lightweight local web UI. See
// ARCHITECTURE.md §6.
//
// Phase 0 skeleton — connects to nothing yet.
package main

import (
	"log"
)

func main() {
	log.Println("velkro: VelkroGo client (Phase 0 skeleton) — not yet wired")
}
